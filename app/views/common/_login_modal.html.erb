<% if !current_user
     session[:return_url] = current_url %>

  <div class="modal login" id="modal-login">
    <div class="modal-container login">
      <a class="boxclose" onclick="$('#modal-login').fadeOut();"> <img src="<%= image_path("cancel.svg")%>" alt="cancel"/> </a>
      <a href="/auth/linkedin" class="linkedin-button">
        <div class="linkedin-logo">
          <%= image_tag('linkedin-2-filled.png') %>
          <div class="slide"></div>
        </div>
        <span class="linkedin-link">Crie uma conta com o seu Linkedin</span>
      </a>
     <!--
      <div class="modal-divider">
        <span class="modal-divider-text">ou</span>
      </div>

      <div class="account-text">
        Entre com seu e-mail e senha
      </div>

      <div class="sign-up-form">
        <div class="form fullname">
          <label>Seu e-mail</label>
          <input type="text">
        </div>

        <div class="form email">
          <label>Sua senha</label>
          <input type="password">
        </div>
      </div>

      <label class="control remindme-checkbox">Lembrar de mim
        <input type="checkbox" id=remindme-login"">
        <div class="control_indicator"></div>
      </label>

      <input value="Entrar" type="submit" class="modal-subimit-button">

      <a class="forgot-password-link" onclick="$('#modal-login').hide(); $('#modal-forgot-password').fadeIn();">
        Esqueceu a senha?
      </a>

      <span class="subimit-notice">
        Você não possui uma conta? <a href="#" onclick="$('#modal-login').hide(); $('#modal-sign-up').fadeIn();">Cadastre-se</a>
      </span>
      -->
    </div>
  </div>

  <script>
  const loginModal = document.getElementById('modal-login');
  window.onclick = event => {
    if (event.target == loginModal) {
      $('#modal-login').fadeOut();
    }
  }
  </script>
<% end %>
